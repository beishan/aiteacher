# 需求开发记录

本文件按时间倒序记录每次需求开发的目标、实现内容和验证结果，用于追踪需求与代码变更之间的关系。

## 记录规范

- 每次完成代码、配置或文档改动后，在文件顶部追加一条记录。
- 每条记录至少包含需求、开发内容、验证结果、分支和关联提交。
- 同一需求的补充修改可以合并到同一日期下，但需保留各项具体内容。
- 不记录密码、令牌、私钥及其他敏感信息。

---

## 2026-08-31｜用户角色与权限管理

### 需求

- 开发系统用户角色与权限功能，支持按角色控制业务操作、系统配置、用户管理和数据备份。

### 开发内容

- 新增管理员、教师、只读用户三档角色：管理员拥有完整权限，教师可维护教学业务，只读用户仅可读取业务数据并维护个人资料。
- 新建用户时可选择角色，用户列表支持管理员直接调整其他账号角色；服务端禁止修改当前登录账号角色，并确保系统始终保留至少一个已启用管理员。
- 在 Spring Security 统一实施接口授权，用户管理、系统设置、系统信息和数据备份仅限管理员；业务写操作限管理员和教师；只读用户仅放行业务查询，并返回统一的 403 JSON 响应。
- 新增界面偏好白名单接口，普通角色可加载主题、内容宽度、Dock 和站点图标，但无法读取 AI 密钥等敏感系统配置。
- 前端增加角色定义、角色选择卡片、角色更新入口、管理菜单与备份路由过滤，以及只读模式状态提示；设置页按角色展示个人、教学或系统管理功能。
- 为数据库用户角色增加取值约束，只允许 `ADMIN`、`TEACHER`、`VIEWER`。

### 验证

- 前端生产构建通过。
- `UserManagementServiceTest` 与 `SettingsControllerTest` 共 7 项测试全部通过，后端 160 个源文件编译通过。
- Vue 类型检查中，本次用户管理新增行类型问题已修复；项目其他页面仍存在既有的 Element Plus 表格行类型、未使用声明等错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次用户角色与权限管理提交

---

## 2026-08-31｜全局弹窗遮挡问题修复

### 需求

- 修复系统用户管理中“重置用户密码”弹窗被页面内容遮盖的问题，并排查其他业务弹窗是否存在同类风险。

### 开发内容

- 将 Element Plus 全局浮层起始层级提升至业务页面最高自定义层级之上，统一覆盖 macOS Dock、资料右键菜单及主题玻璃合成层；弹窗、下拉框、日期选择器和提示框继续使用组件内部动态递增层级。
- 将全部业务对话框遮罩固定为完整视口独立层，避免页面入场动画、滚动容器和 `backdrop-filter` 合成层覆盖弹窗。
- 排查前端全部 20 个 `el-dialog`，补齐科目管理弹窗缺失的 `body` 挂载，并适配视口宽度和居中显示。
- 移除学生表单原有的孤立硬编码层级，改为使用全局弹层策略，避免弹窗内下拉控件反被固定层级遮挡。

### 验证

- 前端生产构建通过，产物包含全局浮层基准和完整视口遮罩规则。
- 静态审计确认 18 个组件内的 20 个 `el-dialog` 均已配置 `append-to-body`。
- 检查高层级页面元素，现有 macOS Dock（2000）及资料右键菜单（9999/10000）均低于新的 Element Plus 浮层基准（12000）。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次全局弹窗遮挡问题修复提交

---

## 2026-08-31｜系统用户管理支持新建用户

### 需求

- 在系统用户管理中增加新建用户功能。

### 开发内容

- 在用户管理工具栏增加“新建用户”入口，并提供登录用户名、显示名称、初始密码和确认密码表单；新账号创建成功后即时追加到用户列表。
- 新建弹窗沿用现有 Element Plus 与主题变量，增加管理员账号说明、字段级校验和移动端单列布局。
- 新增管理员专用的系统用户创建接口，统一创建默认启用的管理员账号，并对用户名格式、显示名称和密码长度进行服务端校验。
- 创建前校验用户名唯一性，初始密码使用 BCrypt 加密后存储，接口响应不包含密码信息。
- 新增用户管理服务单元测试，覆盖成功创建、重复用户名拦截和非管理员拒绝场景。

### 验证

- 前端生产构建通过。
- `UserManagementServiceTest` 共 3 项测试全部通过，后端新增代码编译通过。
- 后端全量测试运行到 22 项时有 17 项既有 WebMvc 测试因缺少 `JwtUtil` Bean 而无法加载测试上下文；本次新增服务测试不受影响。
- Vue 类型检查仍存在项目既有的未使用声明、Element Plus 表格行类型等错误，本次新建用户表单与 API 未新增独立类型错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统用户管理支持新建用户提交

---

## 2026-08-28｜资料显示模式滑块切换

### 需求

- 将资料页面切换显示模式的三个按钮改为滑块式切换控件。

### 开发内容

- 使用 Element Plus `el-segmented` 替换网格、列表、分栏三个独立按钮，保留原有视图状态和内容切换逻辑。
- 每个分段增加对应图标和文字标签，提升显示模式的辨识度与可访问性。
- 增加蓝色渐变选中滑块、半透明轨道、内高光和阴影，并为 macOS 26 主题适配液态玻璃效果。

### 验证

- 前端生产构建通过，生产产物已包含 Element Plus 分段控件及资料页定制样式。
- Vue 类型检查确认本次新增的分段选项和插槽类型无报错；项目其他页面仍存在既有类型错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次资料显示模式滑块切换提交

---

## 2026-08-27｜新增学生弹窗层级修复

### 需求

- 修复点击“新增学生”后，弹窗被学生列表页面内容覆盖的问题。

### 开发内容

- 为新增/编辑学生弹窗增加专用遮罩类，并明确挂载到页面 `body`。
- 将学生弹窗遮罩固定为完整视口尺寸，设置独立顶层层级，避免主题动画、玻璃效果和底部 Dock 的层叠上下文覆盖弹窗。
- 保留弹窗视口居中、主体内部滚动以及标题栏、操作栏常驻行为。

### 验证

- 前端生产构建通过。
- 检查生产编译结果，确认学生弹窗输出专用遮罩类、`body` 挂载目标及 3000 层级，遮罩样式固定覆盖完整动态视口。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次新增学生弹窗层级修复提交

---

## 2026-08-27｜macOS 26 Dock 头像菜单定位修复

### 需求

- 修复 Dock 栏头像点击后不再显示用户菜单的问题。

### 开发内容

- 定位到菜单实际已触发，但 Popper 使用绝对定位时受 Dock 底部布局影响，被计算到视口上方不可见区域。
- 关闭头像 Dropdown 的 Teleport，让弹层直接使用 Dock 自身的稳定坐标系向上展开，避免挂载到尺寸异常的页面根弹层容器后产生负坐标。

### 验证

- 修复前通过浏览器确认头像点击事件和菜单内容均已触发，但 Teleport 弹层被定位到视口上方负坐标区域。
- 检查生产编译结果确认头像 Dropdown 已输出 `teleported: false`，菜单改为留在 Dock 坐标系；原有“系统设置”和“退出登录”命令处理保持不变。
- 前端生产构建和 Docker 镜像构建通过，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 macOS 26 Dock 头像菜单定位修复提交

---

## 2026-08-27｜排课日历月周日滑块切换

### 需求

- 将排课页面日历视图的月、周、日按钮切换改为滑块式分段切换控件。

### 开发内容

- 使用 Element Plus `el-segmented` 替换原有三个独立按钮，选中滑块在月、周、日之间平滑移动。
- 增加紧凑的三等分布局、系统蓝渐变滑块、半透明轨道、内高光与柔和阴影，并对 macOS 26 主题补充液态玻璃效果。
- 沿用原有 FullCalendar 视图切换和本地保存逻辑，切换后继续保存视图类型并在刷新时恢复。

### 验证

- 浏览器验证月、周、日三种视图均可通过滑块切换，选中状态和日历内容同步更新，刷新后保持最后选择。
- 前端生产构建和 Docker 镜像构建通过，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次排课日历月周日滑块切换提交

---

## 2026-08-27｜macOS 26 Dock 图标焦点背景清理

### 需求

- 修复 Dock 图标获得鼠标焦点后出现白色圆角背景框的问题。

### 开发内容

- 覆盖 Element Plus 文字按钮在 Dock 中的悬浮、焦点、可见焦点和按压状态，强制保持边框、背景及阴影透明。
- 移除按钮级方框焦点轮廓，将键盘焦点提示改为图标本体的轻量系统蓝光晕，保留可访问性反馈且不破坏 Dock 图标外观。

### 验证

- 浏览器点击 Dock“成绩”图标后检查焦点状态，背景、边框、阴影和外框均保持透明或无样式。
- 前端生产构建和 Docker 镜像构建通过，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 macOS 26 Dock 图标焦点背景清理提交

---

## 2026-08-27｜macOS 26 Dock 底部定位修复

### 需求

- 修复 macOS 26 主题下 Dock 栏跑到页面顶部的问题，确保其始终位于视口底部。

### 开发内容

- 将 Dock 改为相对 macOS 26 视口布局绝对定位，使其独立于内部内容滚动，避免固定元素在全页渲染状态下被放到页面顶部。
- 使用完整 `inset` 约束显式清除顶部和右侧定位，仅保留底部安全区与水平居中锚点。
- 同步调整移动端定位规则，明确使用底部安全区以及左右 8px 间距，避免继承或全局样式改变 Dock 的垂直位置。

### 验证

- 浏览器验证首页初始状态及页面滚动后 Dock 均保持在视口布局底部。
- 前端生产构建通过，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 macOS 26 Dock 底部定位修复提交

---

## 2026-08-27｜macOS 26 按钮体系统一

### 需求

- 优化 macOS 26 主题下与主题视觉不协调的按钮样式。

### 开发内容

- 统一普通按钮为半透明玻璃渐变、浅边框、内高光和柔和投影，修复默认按钮仍显示 Element Plus 纯白样式的问题。
- 主操作统一采用 macOS 系统蓝；成功、警告和危险操作改为低饱和绿色、橙色和红色玻璃样式，避免大面积高饱和色块。
- 统一文字按钮为系统蓝和系统红，补充轻量悬浮底色；优化朴素按钮、禁用按钮及按压反馈。
- 将月/周/日、视图切换等按钮组调整为玻璃分段控件，未选项透明、选中项使用系统蓝并保留轻阴影。
- 排除 Dock、设置导航、主题卡片、宽度卡片和 Dock 风格卡片等专用交互，避免通用按钮规则覆盖其定制外观。

### 验证

- 前端 `npm run build` 和 Docker 生产镜像构建通过。
- 浏览器抽查首页、学生、排课、成绩、资料和设置页，主按钮、次按钮、语义按钮、文字操作及分段切换样式均已生效。
- 浏览器验证新建文件夹弹窗的取消与创建按钮层级正常，设置专用卡片和 Dock 按钮未受影响。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 macOS 26 按钮体系统一提交

---

## 2026-08-27｜首页今日课程列表 macOS 26 风格优化

### 需求

- 优化首页今日课程列表过于紧凑的问题，使其更舒展并符合 macOS 26 主题。

### 开发内容

- 将紧凑的小尺寸表格重构为独立课程卡片列表，分别展示上课时间、学生、科目和课程状态。
- 桌面端课程卡片使用 84px 基础高度；macOS 26 主题提升至 92px，并增加 19px 圆角、玻璃渐变、半透明边框、内高光和柔和阴影。
- 增加时间图标块、科目胶囊和圆角状态标签，强化信息层级；多课程保持 14px 间距并在限定高度内滚动。
- 新增窄屏双行排版，避免时间、课程信息和状态在小屏幕上相互挤压。

### 验证

- 前端 `npm run build` 和 Docker 生产镜像构建通过。
- 浏览器确认 macOS 26 首页及今日无课程空状态正常；课程卡片结构、主题样式和响应式规则构建生效。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次首页今日课程列表 macOS 26 风格优化提交

---

## 2026-08-27｜成绩页顶部工具栏垂直居中

### 需求

- 修复成绩页面最上方工具栏内容没有上下居中的问题。

### 开发内容

- 移除成绩页工具栏底部为零的非对称内边距，统一设置为上下 16px。
- 保留筛选下拉框、搜索和录入成绩按钮之间原有的水平布局及控件中心对齐。

### 验证

- 前端 `npm run build` 和 Docker 生产镜像构建通过。
- 浏览器测量工具栏表单与卡片内容区的垂直中心偏差由 8.5px 修正为 0，页面显示正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次成绩页顶部工具栏垂直居中提交

---

## 2026-08-27｜设置页左侧菜单尺寸恢复

### 需求

- 将设置页左侧导航恢复为较大的菜单项，解决当前菜单项上下空间过小的问题。

### 开发内容

- 覆盖 Element Plus 按钮默认的 32px 固定高度，将桌面端设置菜单项恢复为 52px 高和 14px 上下内边距。
- 增加图标与文字间距并微调圆角，保持分组、悬浮和选中状态的原有视觉层级。
- 为窄屏横向导航单独设置 44px 高度，兼顾触控面积与横向空间。

### 验证

- 前端 `npm run build` 和 Docker 生产镜像构建通过。
- 浏览器验证桌面端 8 个菜单项高度均为 52px，菜单分组、选中态和内容布局正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次设置页左侧菜单尺寸恢复提交

---

## 2026-08-27｜Dock 图标双层方框修复

### 需求

- 移除 macOS 主题 Dock 图标外多余的方框背景。

### 开发内容

- 移除 Dock 图标容器重复绘制的渐变背景、边框、高光和阴影，仅保留图标组件自身的圆角底板。
- 让内置图标填满原图标容器，并将悬浮、选中时的饱和度和阴影反馈直接应用到图标，保留 Dock 放大交互。
- 同步调整平板和手机断点下的图标尺寸，避免响应式布局重新出现双层底板或图标缩小。

### 验证

- 前端 `npm run build` 和 Docker 生产镜像构建通过。
- 浏览器验证 Dock 容器背景、边框和阴影均已移除，13 个入口正常显示，图标、选中圆点及悬浮效果正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 Dock 图标双层方框修复提交

---

## 2026-08-27｜Dock 图标刷新缓存优化

### 需求

- 修复 macOS 主题 Dock 自定义图标在每次刷新页面时都会重新加载的问题。

### 开发内容

- 将带文件修改时间版本号的 Dock 图标响应由禁止缓存调整为一年期公共不可变缓存。
- 保留图标 URL 的版本参数机制；上传或替换图标后版本号会变化，浏览器会立即获取新资源，未变更的图标则直接复用本地缓存。

### 验证

- Java 21 后端编译及 Dock 图标缓存定向测试通过，Docker 生产镜像构建及服务启动成功。
- 定向测试验证 Dock 图标返回 `public`、一年有效期及 `immutable` 缓存策略；浏览器刷新后 Dock 正常渲染。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 Dock 图标刷新缓存优化提交

---

## 2026-08-26｜Element Plus 组件统一与主题适配

### 需求

- 排查系统中仍使用原生或自定义实现的表格、按钮、输入框、弹窗和通知组件，统一替换为 Element Plus，并适配各套界面主题。

### 开发内容

- 完成前端业务页面组件审计；现有业务表格、表单输入、业务弹窗及消息提示已使用 Element Plus，继续替换设置页主题选项、布局工具栏、用户菜单、通知列表、Dock、头像上传和素材操作中的原生或自定义交互控件。
- 设置页导航、内容宽度卡片、主题卡片和 Dock 图标风格选项统一为 Element Plus 按钮，并保留卡片式视觉与键盘可访问能力。
- 头像文件选择改用 `el-upload`，继续支持选择图片后的裁剪流程；经典、极简、赛博布局的用户菜单统一改用 `el-dropdown`，通知列表改用 Element Plus 按钮和滚动容器。
- 素材名称、收藏、更多操作和右键菜单统一使用 Element Plus；右键菜单采用可独立工作的 `el-menu`，保留鼠标定位、分组、危险操作和主题样式。
- 新增 Element Plus 浮层、通知、上传、菜单的统一主题规则，并分别适配经典、极简、赛博和 macOS 主题的圆角、背景、边框、阴影与交互色。

### 验证

- 前端 `npm run build` 通过，`git diff --check` 通过；源码扫描未发现原生 `button`、`input`、`textarea`、`select`、`table` 或 `dialog` 标签。
- Docker 前端生产镜像构建及服务启动成功。
- 浏览器验证经典主题通知弹层、极简与赛博主题用户菜单、设置页选项与头像上传、素材列表操作及右键菜单均正常，最后恢复 macOS 主题。

### 版本信息

- 分支：`main`
- 关联提交：本次 Element Plus 组件统一与主题适配提交

---

## 2026-08-26｜设置页科目管理与统一科目来源

### 需求

- 在设置中新增科目管理，预设常用科目，并支持新增、修改、删除。
- 排课、学生、作业等业务页面的科目选择统一来源于科目管理。
- 删除科目时检查是否已有排课，避免误删已使用的科目。

### 开发内容

- 新增 V26 数据库迁移和科目目录表，预设语文、数学、英语、物理、化学、生物、历史、政治、地理，并自动收录既有业务数据中的自定义科目。
- 新增科目查询、新增、修改、删除接口；科目名称不区分大小写去重，支持配置显示顺序。
- 科目改名时事务性同步课程、作业、成绩、班级、资料、收费记录和学生学习科目，保持历史业务数据一致。
- 删除前统计课程及其他业务引用；存在排课时返回明确提示，存在其他引用时同样阻止删除，后端作为最终校验。
- 设置页新增“教学 / 科目管理”，展示科目排序、课程数、其他引用数及是否可删除，提供新增和编辑弹窗。
- 排课日历与课程列表、课程表单、学生表单与收费表单、班级、作业、成绩、资料等科目选择全部改为读取统一科目仓库。

### 验证

- 前端 `npm run build` 通过，Java 21 Docker 后端生产构建通过，`git diff --check` 通过。
- 本地容器启动成功，Flyway V26 迁移完成并生成 9 个预设科目。
- 浏览器验证科目列表及使用数量展示正常；新增“科学”后立即出现在排课科目下拉框，改名和删除成功，测试数据已清理。
- 已有 29 节课程的“英语”显示课程及其他引用数量，删除按钮被禁用；后端同时保留排课和其他业务引用校验。

### 版本信息

- 分支：`main`
- 关联提交：本次设置页科目管理与统一科目来源提交

---

## 2026-08-26｜排课页面课程列表

### 需求

- 在排课页面增加页签切换，将原日历放入第一个页签，并在第二个页签新增课程列表。
- 课程列表支持课程状态、学生、排序规则等筛选条件，并提供新增课程按钮。

### 开发内容

- 排课页面新增“日历视图”和“课程列表”两个页签，保留原有月、周、日历操作及视图偏好。
- 课程列表展示上课时间、学生或班级、科目、课程类型、标题、状态、地点，并支持分页。
- 新增课程状态、学生、科目和排序筛选，提供上课时间正序、倒序及最新创建优先三种排序规则。
- 后端课程查询增加白名单排序参数，避免动态字段风险，并保持默认按上课时间升序。
- 列表复用现有新增、详情、编辑、删除、完成和取消流程，课程发生变化后同步刷新日历与列表。

### 验证

- Docker 前端和后端生产构建通过。
- 浏览器验证两个页签切换、列表加载、状态与学生筛选、三种排序规则、分页和新增入口均正常。
- 从课程列表打开详情和编辑弹窗正常，日历页签切回后布局正确，页面及后端日志无错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次排课页面课程列表提交

---

## 2026-08-26｜首页统计数据修复

### 需求

- 修复首页统计数据不准确的问题。

### 开发内容

- 移除首页写死的统计数字和今日课程空状态，接入仪表盘统计接口并增加加载状态。
- 新增本周课程统计，统一以周一至下周一为统计区间，使接口口径与首页“本周课程”标签一致。
- 本月收入改为按业务支付日期统计，并增加下月起始边界，避免按记录创建时间或误计未来记录。
- 今日课程改为查询当天完整时间范围，展示时间、学生、科目和课程状态，并在标题处显示当天课程总数。

### 验证

- Docker 前端和后端生产构建通过。
- 浏览器验证首页显示在读学生 3、本周课程 1、待批作业 0、本月收入 600 元、今日课程 1 节，与数据库只读查询结果一致。
- 今日课程列表正确显示对应学生、时间、科目和状态，页面及后端日志无错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次首页统计数据修复提交

---

## 2026-08-26｜创建班级数据库字段修复

### 需求

- 修复创建班级时查询 `class_members` 表报错，提示缺少 `created_at` 字段的问题。

### 开发内容

- 新增 V25 数据库迁移，为班级成员表补齐基础实体要求的 `created_at` 和 `updated_at` 审计字段。
- 迁移使用兼容现有数据库的条件加列语句，并为已有数据自动填充当前时间，避免升级时出现非空约束问题。
- 保持班级成员实体与全局基础实体字段一致，修复创建班级返回详情、成员列表查询及后续成员写入流程。

### 验证

- Docker 后端构建通过。
- 本地测试容器启动成功，V25 数据库迁移执行完成。
- 浏览器验证创建班级成功、班级卡片正常显示，并删除测试班级恢复数据。
- 后端日志无 SQL 字段错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次创建班级数据库字段修复提交

---

## 2026-08-26｜学生状态编辑入口

### 需求

- 为学生增加状态编辑入口，支持修改“在读”等状态。

### 开发内容

- 在编辑学生弹窗的基本信息区域新增学生状态选项，支持在读、暂停、毕业和流失四种状态。
- 学生列表和学生详情中的编辑入口均接入现有状态更新接口，保存基本资料时同步更新发生变化的状态。
- 新增学生继续默认使用在读状态，不额外增加初始状态选择，保持录入流程简洁。

### 验证

- 前端生产构建通过。
- 浏览器验证列表页编辑弹窗可回显并切换学生状态，保存后列表状态即时更新；测试数据已恢复原状态。
- 页面无控制台错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次学生状态编辑入口提交

---

## 2026-08-26｜新增课程时间范围选择

### 需求

- 将排课页面新增课程弹窗中的开始时间和结束时间改为时间范围选择组件。

### 开发内容

- 将两个独立的日期时间输入合并为一个“课程时间”日期时间范围选择器，统一选择开始与结束时间。
- 保留跨日期选择及精确到分钟的时间设置，提交时继续转换为接口所需的开始时间和结束时间字段。
- 新增时间范围必填校验，并兼容日历点击新增时的默认时段回填及已有课程编辑回显。

### 验证

- 前端生产构建通过。
- 浏览器验证新增课程弹窗仅显示一个日期时间范围组件，开始与结束占位、范围面板和必填校验正常；静态检查确认默认时段、编辑回显及提交字段转换逻辑完整。
- 页面无控制台错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次新增课程时间范围选择提交

---

## 2026-08-26｜排课日历视图偏好保存

### 需求

- 修复排课页面月、周、日视图切换后无法保存的问题。

### 开发内容

- 为排课日历新增月、周、日视图的浏览器本地持久化，切换视图时自动保存当前选择。
- 页面重新进入或刷新时读取并恢复上次使用的日历视图，无有效记录时继续默认使用周视图。
- 日历内部触发视图变化时同步更新顶部切换按钮状态，避免视图与选中状态不一致。

### 验证

- 前端生产构建通过。
- 浏览器验证月、周、日视图切换后刷新页面均能恢复对应视图，顶部按钮选中状态同步正确。
- 页面无控制台错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次排课日历视图偏好保存提交

---

## 2026-08-26｜学生与作业工具栏对齐修复

### 需求

- 修复学生页面和作业页面顶部工具栏按钮没有居中的问题。

### 开发内容

- 移除学生列表与作业列表页面遗留的工具栏底部零内边距覆盖，恢复全站主题样式提供的上下对称间距。
- 使筛选控件和功能按钮在顶部工具栏内保持垂直居中，并继续适配四套界面主题。

### 验证

- 前端生产构建通过。
- 浏览器验证学生与作业页面工具栏上下间距一致，按钮和筛选控件保持垂直居中。
- 页面无控制台错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次学生与作业工具栏对齐修复提交

---

## 2026-08-26｜全站页面与功能按钮主题化优化

### 需求

- 优化各业务页面的功能按钮和页面样式，使界面表现更贴合经典、极简、赛博和 macOS 26 各自的视觉主题。

### 开发内容

- 新增全站业务页面主题增强样式，统一优化搜索栏、工具栏、操作卡片、表格操作区、分段按钮和快捷操作区域的间距、层级与交互反馈。
- 经典主题强化清晰卡片边界、品牌色工具栏强调和语义按钮渐变，保持稳重高效的后台管理风格。
- 极简主题采用轻量描边、留白卡片、胶囊按钮和克制的表格表头，降低视觉噪声。
- 赛博主题增加网格面板、青色霓虹边框与语义色光效，使学生、排课、作业等业务操作融入深色科技界面。
- macOS 26 主题为工具栏、分段控件和操作按钮补充液态玻璃层次、柔和阴影及系统语义色渐变。
- 统一功能按钮的最小点击高度和表格行内操作样式，并补充窄屏布局与减少动态效果的无障碍适配。

### 验证

- Docker 前端生产构建通过，共转换 2344 个模块。
- 本地测试容器更新成功。
- 浏览器逐一切换四套主题，验证学生列表、排课工具栏和首页快捷操作的主题样式均正确生效。
- 720×900 窄屏下业务页面无横向溢出，测试结束后已恢复 macOS 26 主题。
- 页面无控制台错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次全站页面与功能按钮主题化优化提交

---

## 2026-08-26｜新增个人信息设置

### 需求

- 在系统设置中新增个人信息配置，支持编辑头像、修改昵称和维护个人备注。

### 开发内容

- 在设置页新增“账号 / 个人信息”入口，展示当前登录账号、账号角色、昵称和个人备注。
- 支持保存昵称与最长 500 字的个人备注，登录账号保持只读并明确由管理员维护。
- 新增个人头像上传和移除功能，支持 PNG、JPG，限制 5MB，并在服务端自动居中裁剪、标准化为 512×512 PNG。
- 头像选择后先打开圆形裁剪窗口，支持拖动调整位置和滑块缩放，确认裁剪后再生成并上传 512×512 PNG。
- 头像文件按用户隔离存储，读取接口支持无鉴权图片加载并禁用缓存，替换头像后立即刷新显示。
- 个人资料更新结果同步写入用户状态和浏览器缓存，四套主题的顶部栏或 Dock 用户入口均实时显示新昵称和头像。
- 经典主题用户菜单的“个人信息”入口可直接跳转到新设置面板。
- 新增 V24 数据库迁移，为用户表增加个人备注字段；登录和当前用户接口同步返回备注。

### 验证

- Docker 前端生产构建通过，共转换 2344 个模块。
- Docker 后端构建通过，152 个主源文件和 5 个测试源文件编译成功。
- 本地测试容器更新成功，V24 数据库迁移随新后端启动应用。
- 浏览器验证个人信息导航、账号与角色只读状态、昵称和备注保存成功，刷新后数据保持。
- 浏览器上传 1024×1024 PNG 头像成功，个人信息预览和 macOS 26 Dock 用户入口即时显示图片。
- 浏览器验证圆形裁剪框尺寸为 320×320，缩放滑块、图片拖动边界限制和“裁剪并上传”流程均正常。
- 浏览器验证移除头像后恢复昵称首字，清空备注保存后刷新仍为空。
- 测试昵称、备注和头像均已恢复到验证前状态，页面无控制台错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次个人信息设置提交

---

## 2026-08-26｜新增界面主题宽度设置

### 需求

- 在系统设置中新增界面主题宽度配置，支持全屏和居中两种显示方式。
- 全屏保持现有内容铺满可用区域的效果，居中使用与设置页相近的舒适内容宽度。

### 开发内容

- 在“系统外观”的“主题设置”页签新增“界面主题宽度”选项卡，提供全屏和居中两种选择及实时状态反馈。
- 全屏模式保持内容占满布局可用区域；居中模式统一限制内容最大宽度为 1220 像素并水平居中。
- 宽度配置统一覆盖经典、极简、赛博和 macOS 26 四套主题布局，小屏幕下继续自适应可用宽度。
- 新增浏览器本地缓存和服务端设置持久化，应用启动、登录及设置页加载时自动恢复所选宽度。
- 新增 V23 数据库迁移，初始化 `ui.content_width` 为 `full`，保持现有用户界面不变。

### 验证

- Docker 前端生产构建通过；后端镜像构建通过并成功打包 150 个源文件。
- 本地测试容器更新成功，数据库迁移随新后端启动应用。
- 浏览器在 1600×900 视口验证：居中模式内容宽度为 1220 像素，左右各留白 190 像素；刷新后配置保持。
- 切回全屏后内容宽度恢复为 1540 像素、左右仅保留布局内边距；验证结束后已恢复默认全屏设置。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次界面主题宽度设置提交

---

## 2026-08-25｜全站弹窗视口与层级优化

### 需求

- 检查并优化其他页面中弹窗被卡片裁切、被底部 Dock 遮挡或超出视口的问题。

### 开发内容

- 统一为全站 17 个 Element Plus Dialog 启用 Teleport 并挂载到页面 `body`，消除父级卡片、滚动容器和布局层叠上下文的影响。
- 16 个普通弹窗统一使用视口居中布局；OnlyOffice 编辑器保留全屏弹窗行为。
- 为普通弹窗增加视口最大高度限制，内容过长时仅滚动弹窗主体。
- 弹窗标题栏和底部操作栏固定保留，避免长表单的取消、确认等按钮移出视口。
- 网站图标裁剪、学生、班级、排课、作业、成绩、收入、资料预览及编辑、学生费用等弹窗全部覆盖。

### 验证

- Docker 前端生产构建通过，并使用最终镜像更新本地测试容器。
- 静态检查确认 17 个 Dialog 均已启用 Teleport，其中普通弹窗均使用视口居中布局。
- 浏览器验证新增学生长表单高度被限制为 688 像素，主体产生内部滚动，底部操作按钮保持可见。
- 浏览器验证新增收入、用户重置密码和新增课程弹窗均挂载到 `body`，遮罩完整覆盖 1280×720 测试视口。
- 验证弹窗遮罩层级高于 macOS 26 Dock，底部导航不再遮挡弹窗或浮在遮罩上方。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次全站弹窗视口与层级优化提交

---

## 2026-08-25｜修复用户管理弹窗被遮挡

### 需求

- 修复用户管理设置中的重置密码弹窗被内容卡片遮挡、裁切的问题。

### 开发内容

- 将重置密码 Dialog 通过 Element Plus Teleport 挂载到页面 `body`，脱离用户管理卡片的玻璃滤镜定位上下文。
- 启用视口居中布局，使弹窗始终相对浏览器窗口完整显示。
- 保留视口自适应宽度，并确保弹窗遮罩层级高于 macOS 26 底部 Dock。

### 验证

- Docker 前端生产构建通过，并使用最终镜像更新本地测试容器。
- 浏览器复现确认修复前遮罩仅覆盖右侧卡片区域，节点位于用户管理组件内部。
- 修复后遮罩节点挂载于 `body`，覆盖完整的 1280×720 测试视口，弹窗四边及底部操作按钮均完整显示。
- 浏览器计算样式确认弹窗遮罩层级为 2007，高于 Dock 的 2000，不再被底部导航遮挡。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次用户管理弹窗遮挡修复提交

---

## 2026-08-25｜移除自定义 Dock 图标底框

### 需求

- 移除启用自定义 Dock 图标后，底部 Dock 图标下方的方形透明底框。

### 开发内容

- 自定义图标模式下，所有 Dock 导航图标统一移除外层方形底框、边框、背景、圆角和阴影。
- 限制悬浮及当前页面高亮样式，使其不再为自定义图标重新添加方形阴影底框。
- Dock 设置实时预览同步使用无底框样式，确保预览与页面底部实际效果一致。
- 未上传自定义图片的入口也遵循自定义模式的无底框外层样式。

### 验证

- Docker 前端生产构建通过，并使用新镜像更新本地测试容器。
- 浏览器实际启用自定义图标模式后，底部 Dock 与实时预览均不再显示外层方形透明底框。
- 浏览器计算样式确认自定义模式下外层图标容器边框宽度和圆角均为 0，背景及阴影均为 `none`。
- 验证结束后已将测试环境图标风格恢复为原有的 macOS 26。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次移除自定义 Dock 图标底框提交

---

## 2026-08-25｜系统外观页签置顶与滑块样式

### 需求

- 去除系统外观设置内容中的主标题，使页签位于内容区最上方。
- 为页签切换增加圆角矩形底部滑块。

### 开发内容

- 移除系统外观卡片内部的标题、图标和说明区域，保留页面级“系统设置”标题。
- 调整系统外观卡片顶部留白，使“主题设置”和“Dock设置”页签成为内容区首项。
- 重绘 Element Plus 页签活动条，使用主题强调色、5 像素高度、胶囊圆角与柔和阴影。
- 保留 Element Plus 原生页签切换位移，使圆角滑块随当前页签平滑移动。

### 验证

- Docker 前端生产构建通过，并使用新镜像更新本地测试容器。
- 浏览器确认系统外观内容区不再显示重复主标题，最上方直接显示页签。
- 实际切换到“Dock设置”后选中状态正确，滑块从“主题设置”移动到“Dock设置”。
- 浏览器计算样式确认滑块高度为 5 像素、圆角为 999 像素，并正确使用当前主题强调色。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统外观页签样式调整提交

---

## 2026-08-25｜设置页自动保存

### 需求

- 移除设置页面的“保存设置”按钮，配置改为自动保存。

### 开发内容

- 移除设置页顶部的手动保存按钮，新增等待、保存中、已保存和失败状态提示。
- AI 模型与通知配置采用 800 毫秒防抖自动写入服务端，减少连续输入时的重复请求。
- 初次加载服务端配置期间暂停变更监听，避免读取配置触发无意义回写。
- 使用保存代次保护最新状态，避免较早请求覆盖后续修改的显示结果；离开页面前主动提交仍在等待的配置。
- 主题、Dock、网站图标、自定义 Dock 图标和用户管理继续沿用既有即时保存机制。

### 验证

- Docker 前端生产构建通过，并使用最终镜像更新本地测试容器。
- 浏览器确认设置页不再显示“保存设置”按钮，自动保存状态正常显示。
- 将通知提醒时间从 30 分钟改为 35 分钟后，页面依次显示“等待自动保存”和“已自动保存”，设置接口返回值同步为 35。
- 将测试数据恢复为 30 分钟并刷新页面，配置仍为 30，确认服务端持久化及重新加载正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次设置页自动保存提交

---

## 2026-08-25｜macOS 26 默认主题

### 需求

- 将系统默认主题改为 macOS 26。

### 开发内容

- 前端在浏览器没有有效主题缓存时，默认加载 macOS 26 主题和底部 Dock 布局。
- 主布局在遇到无法识别的主题值时改为回退到 macOS 26 布局。
- 新增 V22 数据库迁移，将服务端缺失的主题配置初始化为 `macos26`。
- 对仍使用旧默认值 `default` 的现有部署自动升级为 `macos26`，保留已经选择极简、赛博或 macOS 26 的配置不变。
- 主题继续通过现有服务端设置机制持久化，刷新和重新登录后保持 macOS 26。

### 验证

- Java 21 后端 Docker 跳过测试打包通过，前端 Docker 生产构建通过。
- Flyway 成功将现有数据库从 V21 升级到 V22，主题设置接口返回 `macos26`。
- 浏览器刷新后自动进入 macOS 26 布局，底部 Dock 正常显示。
- 系统外观页面正确标记“当前 · macOS 26”。
- 实际退出并重新登录后仍进入 macOS 26 布局，服务端持久化验证通过。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 macOS 26 默认主题调整提交

---

## 2026-08-25｜系统外观双页签重构

### 需求

- 将设置中的“界面与品牌”改为“系统外观”。
- 将外观配置拆分为“主题设置”和“Dock设置”两个页签。

### 开发内容

- 设置左侧导航和右侧配置面板标题统一调整为“系统外观”。
- 使用 Element Plus Tabs 将系统外观内容拆分为“主题设置”和“Dock设置”两个独立页签。
- 网站图标裁剪上传和主题风格选择归入“主题设置”。
- Dock 实时预览、图标风格、自定义图标及尺寸、透明度、悬浮放大、玻璃模糊配置归入“Dock设置”。
- Dock 配置不再因当前主题不同而隐藏；非 macOS 26 主题下显示生效范围提示，配置仍可提前调整和保存。
- 调整页签与 Dock 面板间距，使新结构适配现有主题和设置页左右布局。

### 验证

- Docker 前端生产构建通过。
- 浏览器实际验证“系统外观”导航及标题、“主题设置”和“Dock设置”切换正常。
- 网站图标与主题卡片仅显示在主题页签，完整 Dock 配置仅显示在 Dock 页签。
- 经典主题下 Dock 生效范围提示正确显示，页面布局无明显溢出。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统外观双页签重构提交

---

## 2026-08-25｜系统信息与服务状态

### 需求

- 在系统设置中新增系统信息，展示系统版本号、服务状态等运行信息。

### 开发内容

- 在设置页左侧“系统”分组新增“系统信息”，提供独立的系统概览面板和手动刷新操作。
- 展示应用版本、当前环境、启动时间、连续运行时长、服务器时间、Java 版本、操作系统、处理器和 JVM 内存使用情况。
- 新增后端服务、PostgreSQL、NAS 本地存储和 OnlyOffice 健康状态检查，并汇总为系统正常或部分服务异常状态。
- PostgreSQL 状态包含安全的版本摘要，NAS 存储状态包含读写能力及可用空间，OnlyOffice 使用短超时健康检查避免阻塞页面。
- 系统信息接口仅返回非敏感诊断数据，不暴露数据库地址、存储路径、服务地址、账号或凭据。
- 系统信息和用户管理页面不再显示无关的“保存设置”按钮，面板按需加载。

### 验证

- Java 21 后端 Docker 跳过测试打包通过，前端 Docker 生产构建通过。
- 未登录访问系统信息接口返回 401，登录后可正常获取版本和完整状态数据。
- 本地环境验证后端、PostgreSQL 和 NAS 存储状态正常；OnlyOffice 响应异常时正确显示部分服务异常。
- 浏览器实际验证系统信息菜单、版本号、运行指标、服务状态、环境详情和刷新按钮正常，布局无明显溢出。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统信息与服务状态功能提交

---

## 2026-08-25｜系统用户管理

### 需求

- 在系统设置中新增系统用户管理功能，支持管理员重置密码以及禁用、启用用户。

### 开发内容

- 在设置页左侧“系统”分组新增“用户管理”，使用 Element Plus 表格展示用户、角色、创建时间和账号状态。
- 新增管理员重置密码弹窗，包含密码长度、确认密码一致性校验。
- 新增用户启用、禁用操作及风险确认，当前登录账号的状态开关不可操作。
- 新增系统用户列表、管理员重置密码和账号状态更新接口，并通过前后端双重管理员权限控制限制访问。
- 新增 V21 数据库迁移，为用户表补充默认启用的账号状态字段。
- 登录时检查账号状态；JWT 鉴权改为实时读取数据库中的最新账号和角色，使禁用操作可立即终止已有登录状态。
- 后端禁止管理员禁用自己，并确保系统始终保留至少一个已启用的管理员账号。

### 验证

- Java 21 后端 Docker 跳过测试打包通过，前端 Docker 生产构建通过。
- Flyway 成功将现有数据库从 V20 升级到 V21。
- 用户列表、禁用、已有令牌立即失效、禁用后拒绝登录、重新启用、密码重置及新密码登录接口闭环验证通过。
- 当前管理员禁用自身时返回业务拒绝，临时测试用户已从本地测试数据库清理。
- 浏览器实际验证用户管理菜单、用户表格、当前账号禁用保护和重置密码弹窗正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统用户管理功能提交

---

## 2026-08-25｜Dock 自定义图标

### 需求

- 在 Dock 设置中增加自定义图标功能。

### 开发内容

- 参考 aibook 的 Dock 图标配置方式，新增“macOS 26 / 自定义”图标风格切换并持久化保存。
- 抽取 12 个 Dock 入口的统一定义，设置预览、图标管理面板和实际 Dock 共用同一份名称、路由、图标及色调配置。
- 为首页、学生、排课、作业、班级、成绩、收入、资料、AI 助手、统计、备份和设置提供独立上传、替换与移除操作。
- 自定义图标支持 PNG、JPG，限制单张 5MB；后端校验图片内容并统一规范为 512×512 PNG。
- 图标保存到现有 NAS 本地持久化卷的 `branding/dock-icons` 目录，不依赖 MinIO。
- 实际 Dock 和设置页实时预览同步显示自定义图片；未上传的入口继续使用内置 macOS 26 图标回退。
- 自定义图标地址带文件版本并缓存到浏览器，刷新时可直接恢复，服务端版本变化后自动更新。
- 新增 V20 数据库迁移保存 Dock 图标风格，应用启动和重新登录后自动恢复风格及图标列表。

### 验证

- Docker 前端生产构建通过。
- Java 21 后端跳过测试打包通过。
- Flyway 成功将现有数据库从 V19 升级到 V20。
- 图标上传、公开读取、512×512 PNG 规范化、未登录上传拦截、重新登录风格持久化及移除接口验证通过。
- 浏览器实际验证图标风格切换、12 个上传槽位、设置预览和底部 Dock 自定义图片即时渲染通过。
- Vue 类型检查未发现本次新增文件错误；项目既有页面仍存在历史类型错误。
- 测试图标、主题和图标风格均已恢复到验证前状态。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 Dock 自定义图标功能提交

---

## 2026-08-25｜新增网站图标裁剪上传设置

### 需求

- 在系统设置中增加网站图标配置，支持选择图片、裁剪并上传。

### 开发内容

- 在“界面与品牌”设置中新增网站图标卡片，支持 PNG、JPG、WebP 图片选择和 5MB 大小限制。
- 新增响应式方形裁剪弹窗，支持拖动定位、滑块缩放，并在浏览器端输出 512×512 PNG。
- 新增网站图标上传和公开读取接口，后端再次校验图片类型、尺寸与内容并规范化为 512×512 PNG。
- 图标文件保存到现有 NAS 本地持久化卷的 `branding` 目录，不依赖 MinIO。
- 新增 V19 数据库迁移保存网站图标地址，应用启动和登录后自动恢复并更新浏览器 favicon。
- 保留浏览器本地地址作为接口暂不可用时的回退，并通过时间戳避免旧图标缓存。

### 验证

- Docker 前端生产构建通过。
- Java 21 后端跳过测试打包通过。
- Flyway 成功将现有数据库从 V18 升级到 V19。
- 上传接口、未登录上传拦截、公开图标读取及设置持久化验证通过。
- 后端输出图标确认为 512×512 PNG。
- 浏览器实际验证图片选择、裁剪弹窗、缩放、裁剪上传、当前图标预览及 favicon 即时更新通过。
- Vue 类型检查未发现本次新增文件错误；项目既有页面仍存在历史类型错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次网站图标裁剪上传功能提交

---

## 2026-08-25｜主题与 Dock 设置持久化

### 需求

- 修复主题设置只保存在当前浏览器、重新登录后发生变化的问题。

### 开发内容

- 新增 V18 数据库迁移，初始化主题及 Dock 尺寸、透明度、放大比例、模糊度等界面设置。
- 主题切换后立即写入系统设置，同时保留浏览器本地设置作为接口不可用时的回退。
- 应用启动及登录成功后从服务器恢复主题和 Dock 设置，确保重新登录后保持一致。
- Dock 滑块修改和恢复默认值时同步保存到服务器，系统设置的统一保存也会包含当前界面配置。
- 更新 Dock 设置提示，明确配置会跨登录保留。

### 验证

- Docker 前端生产构建通过。
- Java 21 后端跳过测试打包通过。
- Flyway 成功将现有数据库从 V17 升级到 V18。
- 接口闭环验证通过：保存 `macos26` 后重新登录，服务器仍返回 `macos26`；验证后已恢复原设置。
- 后端既有控制器测试仍因测试上下文未提供 `JwtUtil` 模拟对象而失败，本次变更无新增测试失败。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次主题与 Dock 持久化修复提交

---

## 2026-08-25｜设置页左右布局与 Dock 视觉重构

### 需求

- 系统设置改成参考 aibook 的左右布局，左侧为设置菜单，右侧为配置内容。
- 缩小当前 macOS 26 Dock 与 aibook 项目 Dock 的视觉和交互差异。

### 开发内容

- 设置页重构为左侧分组菜单和右侧独立配置面板，移动端自动切换为横向滚动菜单。
- 外观、AI 模型、通知配置、使用统计拆分为独立设置分类。
- 移除 macOS 26 布局中额外增加的固定顶部栏，使页面结构更接近 aibook DockLayout。
- Dock 改为液态玻璃托盘、双层材质图标、活动指示点和用户头像分隔入口。
- 悬浮放大改为根据指针距离连续计算，支持邻近图标平滑联动。
- Dock 设置面板改为 aibook 风格的大尺寸实时预览和带说明的纵向滑块配置。
- 新增可复用的 `MacosDockIcon` 液态玻璃图标组件。

### 验证

- Docker 前端生产构建通过。
- `git diff --check` 通过。
- Vue 类型检查未发现本次新增文件错误；项目既有页面仍存在历史类型错误。
- 桌面端和 390×844 移动端实际浏览器渲染检查通过。
- 设置分类切换正常，Dock 指针距离测试可同时联动缩放 3 个相邻图标。
- 浏览器控制台无错误。

### 版本信息

- 分支：`main`
- 关联提交：本次设置页与 Dock 重构提交

---

## 2026-08-25｜统一使用 main 分支开发

### 需求

- 后续所有需求直接在 `main` 分支开发。

### 开发内容

- 将 macOS 26 主题及开发记录提交快进合并到本地 `main`。
- 更新项目协作约定，要求开发前确认位于 `main`，完成后提交并推送远程 `main`。
- 不再为常规需求创建独立功能分支。

### 验证

- `main` 分支快进合并成功。
- 与需求无关的 `.env.production.example` 本地修改未纳入提交。

### 版本信息

- 分支：`main`
- 关联提交：本次协作约定提交

---

## 2026-08-25｜建立需求开发记录机制

### 需求

- 增加开发记录，持续记录每次开发内容。

### 开发内容

- 新增 `DEVELOPMENT_LOG.md`，统一记录需求、实现、验证、分支及提交信息。
- 在 `README.md` 中增加开发记录入口。
- 增加项目协作约定，要求后续每次开发完成后更新本记录并自动提交、推送当前远程分支。

### 验证

- Markdown 格式检查通过。
- Git 差异检查通过。

### 版本信息

- 分支：`main`
- 关联提交：本次文档提交

---

## 2026-08-25｜新增 macOS 26 主题及可配置 Dock

### 需求

- 参考 aibook 项目的 macOS 26 主题，为家教助手新增同类主题。
- 尽量使用 Element Plus 组件，覆盖全部页面。
- 使用底部 Dock 导航，并提供 Dock 设置项。

### 开发内容

- 新增 macOS 26 Liquid Glass 独立布局、顶部玻璃栏和底部 Dock。
- Dock 覆盖首页、学生、排课、作业、班级、成绩、收入、资料、AI 助手、统计、备份和设置入口。
- 增加图标大小、背景透明度、悬停放大、玻璃模糊四项 Dock 设置及恢复默认功能。
- 增加 macOS 26 主题变量和 Element Plus 全局适配。
- 覆盖登录页、FullCalendar、聊天、资料预览、业务卡片及移动端布局。

### 验证

- Docker 前端生产构建通过。
- `git diff --check` 通过。
- Vue 类型检查未发现新增主题文件错误；项目其他既有页面仍存在历史类型错误。

### 版本信息

- 分支：`main`
- 关联提交：`5a552d8 feat(frontend): add macOS 26 theme and configurable dock`

---

## 2026-08-25｜飞牛 NAS Jenkins 部署、Java 21 与本地文件存储

### 需求

- 参考 aibook 项目，在家庭局域网飞牛 NAS 的 Docker Jenkins 中部署项目。
- 后端升级为 Java 21。
- 去除 MinIO 和 Redis 依赖。

### 开发内容

- 调整 Jenkins 流水线、生产 Docker Compose、部署脚本和飞牛 NAS 部署文档。
- 后端构建及运行环境升级到 Java 21。
- 删除 MinIO 配置和服务，资料文件改用 NAS Docker 持久化卷与本地文件存储服务。
- 清理 Redis、MinIO 相关配置和编排依赖。
- 增加生产环境配置样例、部署校验、健康检查和失败回滚流程。

### 验证

- 后端测试和打包通过。
- 前后端 Docker 镜像构建通过。
- 生产 Compose 配置及部署脚本校验通过。

### 版本信息

- 分支：`main`
- 关联提交：`99922b2 feat: 优化jenkins部署`
