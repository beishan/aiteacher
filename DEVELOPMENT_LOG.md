# 需求开发记录

本文件按时间倒序记录每次需求开发的目标、实现内容和验证结果，用于追踪需求与代码变更之间的关系。

## 记录规范

- 每次完成代码、配置或文档改动后，在文件顶部追加一条记录。
- 每条记录至少包含需求、开发内容、验证结果、分支和关联提交。
- 同一需求的补充修改可以合并到同一日期下，但需保留各项具体内容。
- 不记录密码、令牌、私钥及其他敏感信息。

---

## 2026-08-26｜全站页面与功能按钮主题化优化

### 需求

- 优化各业务页面的功能按钮和页面样式，使界面表现更贴合经典、极简、赛博和 macOS 26 各自的视觉主题。

### 开发内容

- 新增全站业务页面主题增强样式，统一优化搜索栏、工具栏、操作卡片、表格操作区、分段按钮和快捷操作区域的间距、层级与交互反馈。
- 经典主题强化清晰卡片边界、品牌色工具栏强调和语义按钮渐变，保持稳重高效的后台管理风格。
- 极简主题采用轻量描边、留白卡片、胶囊按钮和克制的表格表头，降低视觉噪声。
- 赛博主题增加网格面板、青色霓虹边框与语义色光效，使学生、排课、作业等业务操作融入深色科技界面。
- macOS 26 主题为工具栏、分段控件和操作按钮补充液态玻璃层次、柔和阴影及系统语义色渐变。
- 统一功能按钮的最小点击高度和表格行内操作样式，并补充窄屏布局与减少动态效果的无障碍适配。

### 验证

- Docker 前端生产构建通过，共转换 2344 个模块。
- 本地测试容器更新成功。
- 浏览器逐一切换四套主题，验证学生列表、排课工具栏和首页快捷操作的主题样式均正确生效。
- 720×900 窄屏下业务页面无横向溢出，测试结束后已恢复 macOS 26 主题。
- 页面无控制台错误，`git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次全站页面与功能按钮主题化优化提交

---

## 2026-08-26｜新增个人信息设置

### 需求

- 在系统设置中新增个人信息配置，支持编辑头像、修改昵称和维护个人备注。

### 开发内容

- 在设置页新增“账号 / 个人信息”入口，展示当前登录账号、账号角色、昵称和个人备注。
- 支持保存昵称与最长 500 字的个人备注，登录账号保持只读并明确由管理员维护。
- 新增个人头像上传和移除功能，支持 PNG、JPG，限制 5MB，并在服务端自动居中裁剪、标准化为 512×512 PNG。
- 头像选择后先打开圆形裁剪窗口，支持拖动调整位置和滑块缩放，确认裁剪后再生成并上传 512×512 PNG。
- 头像文件按用户隔离存储，读取接口支持无鉴权图片加载并禁用缓存，替换头像后立即刷新显示。
- 个人资料更新结果同步写入用户状态和浏览器缓存，四套主题的顶部栏或 Dock 用户入口均实时显示新昵称和头像。
- 经典主题用户菜单的“个人信息”入口可直接跳转到新设置面板。
- 新增 V24 数据库迁移，为用户表增加个人备注字段；登录和当前用户接口同步返回备注。

### 验证

- Docker 前端生产构建通过，共转换 2344 个模块。
- Docker 后端构建通过，152 个主源文件和 5 个测试源文件编译成功。
- 本地测试容器更新成功，V24 数据库迁移随新后端启动应用。
- 浏览器验证个人信息导航、账号与角色只读状态、昵称和备注保存成功，刷新后数据保持。
- 浏览器上传 1024×1024 PNG 头像成功，个人信息预览和 macOS 26 Dock 用户入口即时显示图片。
- 浏览器验证圆形裁剪框尺寸为 320×320，缩放滑块、图片拖动边界限制和“裁剪并上传”流程均正常。
- 浏览器验证移除头像后恢复昵称首字，清空备注保存后刷新仍为空。
- 测试昵称、备注和头像均已恢复到验证前状态，页面无控制台错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次个人信息设置提交

---

## 2026-08-26｜新增界面主题宽度设置

### 需求

- 在系统设置中新增界面主题宽度配置，支持全屏和居中两种显示方式。
- 全屏保持现有内容铺满可用区域的效果，居中使用与设置页相近的舒适内容宽度。

### 开发内容

- 在“系统外观”的“主题设置”页签新增“界面主题宽度”选项卡，提供全屏和居中两种选择及实时状态反馈。
- 全屏模式保持内容占满布局可用区域；居中模式统一限制内容最大宽度为 1220 像素并水平居中。
- 宽度配置统一覆盖经典、极简、赛博和 macOS 26 四套主题布局，小屏幕下继续自适应可用宽度。
- 新增浏览器本地缓存和服务端设置持久化，应用启动、登录及设置页加载时自动恢复所选宽度。
- 新增 V23 数据库迁移，初始化 `ui.content_width` 为 `full`，保持现有用户界面不变。

### 验证

- Docker 前端生产构建通过；后端镜像构建通过并成功打包 150 个源文件。
- 本地测试容器更新成功，数据库迁移随新后端启动应用。
- 浏览器在 1600×900 视口验证：居中模式内容宽度为 1220 像素，左右各留白 190 像素；刷新后配置保持。
- 切回全屏后内容宽度恢复为 1540 像素、左右仅保留布局内边距；验证结束后已恢复默认全屏设置。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次界面主题宽度设置提交

---

## 2026-08-25｜全站弹窗视口与层级优化

### 需求

- 检查并优化其他页面中弹窗被卡片裁切、被底部 Dock 遮挡或超出视口的问题。

### 开发内容

- 统一为全站 17 个 Element Plus Dialog 启用 Teleport 并挂载到页面 `body`，消除父级卡片、滚动容器和布局层叠上下文的影响。
- 16 个普通弹窗统一使用视口居中布局；OnlyOffice 编辑器保留全屏弹窗行为。
- 为普通弹窗增加视口最大高度限制，内容过长时仅滚动弹窗主体。
- 弹窗标题栏和底部操作栏固定保留，避免长表单的取消、确认等按钮移出视口。
- 网站图标裁剪、学生、班级、排课、作业、成绩、收入、资料预览及编辑、学生费用等弹窗全部覆盖。

### 验证

- Docker 前端生产构建通过，并使用最终镜像更新本地测试容器。
- 静态检查确认 17 个 Dialog 均已启用 Teleport，其中普通弹窗均使用视口居中布局。
- 浏览器验证新增学生长表单高度被限制为 688 像素，主体产生内部滚动，底部操作按钮保持可见。
- 浏览器验证新增收入、用户重置密码和新增课程弹窗均挂载到 `body`，遮罩完整覆盖 1280×720 测试视口。
- 验证弹窗遮罩层级高于 macOS 26 Dock，底部导航不再遮挡弹窗或浮在遮罩上方。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次全站弹窗视口与层级优化提交

---

## 2026-08-25｜修复用户管理弹窗被遮挡

### 需求

- 修复用户管理设置中的重置密码弹窗被内容卡片遮挡、裁切的问题。

### 开发内容

- 将重置密码 Dialog 通过 Element Plus Teleport 挂载到页面 `body`，脱离用户管理卡片的玻璃滤镜定位上下文。
- 启用视口居中布局，使弹窗始终相对浏览器窗口完整显示。
- 保留视口自适应宽度，并确保弹窗遮罩层级高于 macOS 26 底部 Dock。

### 验证

- Docker 前端生产构建通过，并使用最终镜像更新本地测试容器。
- 浏览器复现确认修复前遮罩仅覆盖右侧卡片区域，节点位于用户管理组件内部。
- 修复后遮罩节点挂载于 `body`，覆盖完整的 1280×720 测试视口，弹窗四边及底部操作按钮均完整显示。
- 浏览器计算样式确认弹窗遮罩层级为 2007，高于 Dock 的 2000，不再被底部导航遮挡。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次用户管理弹窗遮挡修复提交

---

## 2026-08-25｜移除自定义 Dock 图标底框

### 需求

- 移除启用自定义 Dock 图标后，底部 Dock 图标下方的方形透明底框。

### 开发内容

- 自定义图标模式下，所有 Dock 导航图标统一移除外层方形底框、边框、背景、圆角和阴影。
- 限制悬浮及当前页面高亮样式，使其不再为自定义图标重新添加方形阴影底框。
- Dock 设置实时预览同步使用无底框样式，确保预览与页面底部实际效果一致。
- 未上传自定义图片的入口也遵循自定义模式的无底框外层样式。

### 验证

- Docker 前端生产构建通过，并使用新镜像更新本地测试容器。
- 浏览器实际启用自定义图标模式后，底部 Dock 与实时预览均不再显示外层方形透明底框。
- 浏览器计算样式确认自定义模式下外层图标容器边框宽度和圆角均为 0，背景及阴影均为 `none`。
- 验证结束后已将测试环境图标风格恢复为原有的 macOS 26。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次移除自定义 Dock 图标底框提交

---

## 2026-08-25｜系统外观页签置顶与滑块样式

### 需求

- 去除系统外观设置内容中的主标题，使页签位于内容区最上方。
- 为页签切换增加圆角矩形底部滑块。

### 开发内容

- 移除系统外观卡片内部的标题、图标和说明区域，保留页面级“系统设置”标题。
- 调整系统外观卡片顶部留白，使“主题设置”和“Dock设置”页签成为内容区首项。
- 重绘 Element Plus 页签活动条，使用主题强调色、5 像素高度、胶囊圆角与柔和阴影。
- 保留 Element Plus 原生页签切换位移，使圆角滑块随当前页签平滑移动。

### 验证

- Docker 前端生产构建通过，并使用新镜像更新本地测试容器。
- 浏览器确认系统外观内容区不再显示重复主标题，最上方直接显示页签。
- 实际切换到“Dock设置”后选中状态正确，滑块从“主题设置”移动到“Dock设置”。
- 浏览器计算样式确认滑块高度为 5 像素、圆角为 999 像素，并正确使用当前主题强调色。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统外观页签样式调整提交

---

## 2026-08-25｜设置页自动保存

### 需求

- 移除设置页面的“保存设置”按钮，配置改为自动保存。

### 开发内容

- 移除设置页顶部的手动保存按钮，新增等待、保存中、已保存和失败状态提示。
- AI 模型与通知配置采用 800 毫秒防抖自动写入服务端，减少连续输入时的重复请求。
- 初次加载服务端配置期间暂停变更监听，避免读取配置触发无意义回写。
- 使用保存代次保护最新状态，避免较早请求覆盖后续修改的显示结果；离开页面前主动提交仍在等待的配置。
- 主题、Dock、网站图标、自定义 Dock 图标和用户管理继续沿用既有即时保存机制。

### 验证

- Docker 前端生产构建通过，并使用最终镜像更新本地测试容器。
- 浏览器确认设置页不再显示“保存设置”按钮，自动保存状态正常显示。
- 将通知提醒时间从 30 分钟改为 35 分钟后，页面依次显示“等待自动保存”和“已自动保存”，设置接口返回值同步为 35。
- 将测试数据恢复为 30 分钟并刷新页面，配置仍为 30，确认服务端持久化及重新加载正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次设置页自动保存提交

---

## 2026-08-25｜macOS 26 默认主题

### 需求

- 将系统默认主题改为 macOS 26。

### 开发内容

- 前端在浏览器没有有效主题缓存时，默认加载 macOS 26 主题和底部 Dock 布局。
- 主布局在遇到无法识别的主题值时改为回退到 macOS 26 布局。
- 新增 V22 数据库迁移，将服务端缺失的主题配置初始化为 `macos26`。
- 对仍使用旧默认值 `default` 的现有部署自动升级为 `macos26`，保留已经选择极简、赛博或 macOS 26 的配置不变。
- 主题继续通过现有服务端设置机制持久化，刷新和重新登录后保持 macOS 26。

### 验证

- Java 21 后端 Docker 跳过测试打包通过，前端 Docker 生产构建通过。
- Flyway 成功将现有数据库从 V21 升级到 V22，主题设置接口返回 `macos26`。
- 浏览器刷新后自动进入 macOS 26 布局，底部 Dock 正常显示。
- 系统外观页面正确标记“当前 · macOS 26”。
- 实际退出并重新登录后仍进入 macOS 26 布局，服务端持久化验证通过。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 macOS 26 默认主题调整提交

---

## 2026-08-25｜系统外观双页签重构

### 需求

- 将设置中的“界面与品牌”改为“系统外观”。
- 将外观配置拆分为“主题设置”和“Dock设置”两个页签。

### 开发内容

- 设置左侧导航和右侧配置面板标题统一调整为“系统外观”。
- 使用 Element Plus Tabs 将系统外观内容拆分为“主题设置”和“Dock设置”两个独立页签。
- 网站图标裁剪上传和主题风格选择归入“主题设置”。
- Dock 实时预览、图标风格、自定义图标及尺寸、透明度、悬浮放大、玻璃模糊配置归入“Dock设置”。
- Dock 配置不再因当前主题不同而隐藏；非 macOS 26 主题下显示生效范围提示，配置仍可提前调整和保存。
- 调整页签与 Dock 面板间距，使新结构适配现有主题和设置页左右布局。

### 验证

- Docker 前端生产构建通过。
- 浏览器实际验证“系统外观”导航及标题、“主题设置”和“Dock设置”切换正常。
- 网站图标与主题卡片仅显示在主题页签，完整 Dock 配置仅显示在 Dock 页签。
- 经典主题下 Dock 生效范围提示正确显示，页面布局无明显溢出。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统外观双页签重构提交

---

## 2026-08-25｜系统信息与服务状态

### 需求

- 在系统设置中新增系统信息，展示系统版本号、服务状态等运行信息。

### 开发内容

- 在设置页左侧“系统”分组新增“系统信息”，提供独立的系统概览面板和手动刷新操作。
- 展示应用版本、当前环境、启动时间、连续运行时长、服务器时间、Java 版本、操作系统、处理器和 JVM 内存使用情况。
- 新增后端服务、PostgreSQL、NAS 本地存储和 OnlyOffice 健康状态检查，并汇总为系统正常或部分服务异常状态。
- PostgreSQL 状态包含安全的版本摘要，NAS 存储状态包含读写能力及可用空间，OnlyOffice 使用短超时健康检查避免阻塞页面。
- 系统信息接口仅返回非敏感诊断数据，不暴露数据库地址、存储路径、服务地址、账号或凭据。
- 系统信息和用户管理页面不再显示无关的“保存设置”按钮，面板按需加载。

### 验证

- Java 21 后端 Docker 跳过测试打包通过，前端 Docker 生产构建通过。
- 未登录访问系统信息接口返回 401，登录后可正常获取版本和完整状态数据。
- 本地环境验证后端、PostgreSQL 和 NAS 存储状态正常；OnlyOffice 响应异常时正确显示部分服务异常。
- 浏览器实际验证系统信息菜单、版本号、运行指标、服务状态、环境详情和刷新按钮正常，布局无明显溢出。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统信息与服务状态功能提交

---

## 2026-08-25｜系统用户管理

### 需求

- 在系统设置中新增系统用户管理功能，支持管理员重置密码以及禁用、启用用户。

### 开发内容

- 在设置页左侧“系统”分组新增“用户管理”，使用 Element Plus 表格展示用户、角色、创建时间和账号状态。
- 新增管理员重置密码弹窗，包含密码长度、确认密码一致性校验。
- 新增用户启用、禁用操作及风险确认，当前登录账号的状态开关不可操作。
- 新增系统用户列表、管理员重置密码和账号状态更新接口，并通过前后端双重管理员权限控制限制访问。
- 新增 V21 数据库迁移，为用户表补充默认启用的账号状态字段。
- 登录时检查账号状态；JWT 鉴权改为实时读取数据库中的最新账号和角色，使禁用操作可立即终止已有登录状态。
- 后端禁止管理员禁用自己，并确保系统始终保留至少一个已启用的管理员账号。

### 验证

- Java 21 后端 Docker 跳过测试打包通过，前端 Docker 生产构建通过。
- Flyway 成功将现有数据库从 V20 升级到 V21。
- 用户列表、禁用、已有令牌立即失效、禁用后拒绝登录、重新启用、密码重置及新密码登录接口闭环验证通过。
- 当前管理员禁用自身时返回业务拒绝，临时测试用户已从本地测试数据库清理。
- 浏览器实际验证用户管理菜单、用户表格、当前账号禁用保护和重置密码弹窗正常。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次系统用户管理功能提交

---

## 2026-08-25｜Dock 自定义图标

### 需求

- 在 Dock 设置中增加自定义图标功能。

### 开发内容

- 参考 aibook 的 Dock 图标配置方式，新增“macOS 26 / 自定义”图标风格切换并持久化保存。
- 抽取 12 个 Dock 入口的统一定义，设置预览、图标管理面板和实际 Dock 共用同一份名称、路由、图标及色调配置。
- 为首页、学生、排课、作业、班级、成绩、收入、资料、AI 助手、统计、备份和设置提供独立上传、替换与移除操作。
- 自定义图标支持 PNG、JPG，限制单张 5MB；后端校验图片内容并统一规范为 512×512 PNG。
- 图标保存到现有 NAS 本地持久化卷的 `branding/dock-icons` 目录，不依赖 MinIO。
- 实际 Dock 和设置页实时预览同步显示自定义图片；未上传的入口继续使用内置 macOS 26 图标回退。
- 自定义图标地址带文件版本并缓存到浏览器，刷新时可直接恢复，服务端版本变化后自动更新。
- 新增 V20 数据库迁移保存 Dock 图标风格，应用启动和重新登录后自动恢复风格及图标列表。

### 验证

- Docker 前端生产构建通过。
- Java 21 后端跳过测试打包通过。
- Flyway 成功将现有数据库从 V19 升级到 V20。
- 图标上传、公开读取、512×512 PNG 规范化、未登录上传拦截、重新登录风格持久化及移除接口验证通过。
- 浏览器实际验证图标风格切换、12 个上传槽位、设置预览和底部 Dock 自定义图片即时渲染通过。
- Vue 类型检查未发现本次新增文件错误；项目既有页面仍存在历史类型错误。
- 测试图标、主题和图标风格均已恢复到验证前状态。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次 Dock 自定义图标功能提交

---

## 2026-08-25｜新增网站图标裁剪上传设置

### 需求

- 在系统设置中增加网站图标配置，支持选择图片、裁剪并上传。

### 开发内容

- 在“界面与品牌”设置中新增网站图标卡片，支持 PNG、JPG、WebP 图片选择和 5MB 大小限制。
- 新增响应式方形裁剪弹窗，支持拖动定位、滑块缩放，并在浏览器端输出 512×512 PNG。
- 新增网站图标上传和公开读取接口，后端再次校验图片类型、尺寸与内容并规范化为 512×512 PNG。
- 图标文件保存到现有 NAS 本地持久化卷的 `branding` 目录，不依赖 MinIO。
- 新增 V19 数据库迁移保存网站图标地址，应用启动和登录后自动恢复并更新浏览器 favicon。
- 保留浏览器本地地址作为接口暂不可用时的回退，并通过时间戳避免旧图标缓存。

### 验证

- Docker 前端生产构建通过。
- Java 21 后端跳过测试打包通过。
- Flyway 成功将现有数据库从 V18 升级到 V19。
- 上传接口、未登录上传拦截、公开图标读取及设置持久化验证通过。
- 后端输出图标确认为 512×512 PNG。
- 浏览器实际验证图片选择、裁剪弹窗、缩放、裁剪上传、当前图标预览及 favicon 即时更新通过。
- Vue 类型检查未发现本次新增文件错误；项目既有页面仍存在历史类型错误。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次网站图标裁剪上传功能提交

---

## 2026-08-25｜主题与 Dock 设置持久化

### 需求

- 修复主题设置只保存在当前浏览器、重新登录后发生变化的问题。

### 开发内容

- 新增 V18 数据库迁移，初始化主题及 Dock 尺寸、透明度、放大比例、模糊度等界面设置。
- 主题切换后立即写入系统设置，同时保留浏览器本地设置作为接口不可用时的回退。
- 应用启动及登录成功后从服务器恢复主题和 Dock 设置，确保重新登录后保持一致。
- Dock 滑块修改和恢复默认值时同步保存到服务器，系统设置的统一保存也会包含当前界面配置。
- 更新 Dock 设置提示，明确配置会跨登录保留。

### 验证

- Docker 前端生产构建通过。
- Java 21 后端跳过测试打包通过。
- Flyway 成功将现有数据库从 V17 升级到 V18。
- 接口闭环验证通过：保存 `macos26` 后重新登录，服务器仍返回 `macos26`；验证后已恢复原设置。
- 后端既有控制器测试仍因测试上下文未提供 `JwtUtil` 模拟对象而失败，本次变更无新增测试失败。
- `git diff --check` 通过。

### 版本信息

- 分支：`main`
- 关联提交：本次主题与 Dock 持久化修复提交

---

## 2026-08-25｜设置页左右布局与 Dock 视觉重构

### 需求

- 系统设置改成参考 aibook 的左右布局，左侧为设置菜单，右侧为配置内容。
- 缩小当前 macOS 26 Dock 与 aibook 项目 Dock 的视觉和交互差异。

### 开发内容

- 设置页重构为左侧分组菜单和右侧独立配置面板，移动端自动切换为横向滚动菜单。
- 外观、AI 模型、通知配置、使用统计拆分为独立设置分类。
- 移除 macOS 26 布局中额外增加的固定顶部栏，使页面结构更接近 aibook DockLayout。
- Dock 改为液态玻璃托盘、双层材质图标、活动指示点和用户头像分隔入口。
- 悬浮放大改为根据指针距离连续计算，支持邻近图标平滑联动。
- Dock 设置面板改为 aibook 风格的大尺寸实时预览和带说明的纵向滑块配置。
- 新增可复用的 `MacosDockIcon` 液态玻璃图标组件。

### 验证

- Docker 前端生产构建通过。
- `git diff --check` 通过。
- Vue 类型检查未发现本次新增文件错误；项目既有页面仍存在历史类型错误。
- 桌面端和 390×844 移动端实际浏览器渲染检查通过。
- 设置分类切换正常，Dock 指针距离测试可同时联动缩放 3 个相邻图标。
- 浏览器控制台无错误。

### 版本信息

- 分支：`main`
- 关联提交：本次设置页与 Dock 重构提交

---

## 2026-08-25｜统一使用 main 分支开发

### 需求

- 后续所有需求直接在 `main` 分支开发。

### 开发内容

- 将 macOS 26 主题及开发记录提交快进合并到本地 `main`。
- 更新项目协作约定，要求开发前确认位于 `main`，完成后提交并推送远程 `main`。
- 不再为常规需求创建独立功能分支。

### 验证

- `main` 分支快进合并成功。
- 与需求无关的 `.env.production.example` 本地修改未纳入提交。

### 版本信息

- 分支：`main`
- 关联提交：本次协作约定提交

---

## 2026-08-25｜建立需求开发记录机制

### 需求

- 增加开发记录，持续记录每次开发内容。

### 开发内容

- 新增 `DEVELOPMENT_LOG.md`，统一记录需求、实现、验证、分支及提交信息。
- 在 `README.md` 中增加开发记录入口。
- 增加项目协作约定，要求后续每次开发完成后更新本记录并自动提交、推送当前远程分支。

### 验证

- Markdown 格式检查通过。
- Git 差异检查通过。

### 版本信息

- 分支：`main`
- 关联提交：本次文档提交

---

## 2026-08-25｜新增 macOS 26 主题及可配置 Dock

### 需求

- 参考 aibook 项目的 macOS 26 主题，为家教助手新增同类主题。
- 尽量使用 Element Plus 组件，覆盖全部页面。
- 使用底部 Dock 导航，并提供 Dock 设置项。

### 开发内容

- 新增 macOS 26 Liquid Glass 独立布局、顶部玻璃栏和底部 Dock。
- Dock 覆盖首页、学生、排课、作业、班级、成绩、收入、资料、AI 助手、统计、备份和设置入口。
- 增加图标大小、背景透明度、悬停放大、玻璃模糊四项 Dock 设置及恢复默认功能。
- 增加 macOS 26 主题变量和 Element Plus 全局适配。
- 覆盖登录页、FullCalendar、聊天、资料预览、业务卡片及移动端布局。

### 验证

- Docker 前端生产构建通过。
- `git diff --check` 通过。
- Vue 类型检查未发现新增主题文件错误；项目其他既有页面仍存在历史类型错误。

### 版本信息

- 分支：`main`
- 关联提交：`5a552d8 feat(frontend): add macOS 26 theme and configurable dock`

---

## 2026-08-25｜飞牛 NAS Jenkins 部署、Java 21 与本地文件存储

### 需求

- 参考 aibook 项目，在家庭局域网飞牛 NAS 的 Docker Jenkins 中部署项目。
- 后端升级为 Java 21。
- 去除 MinIO 和 Redis 依赖。

### 开发内容

- 调整 Jenkins 流水线、生产 Docker Compose、部署脚本和飞牛 NAS 部署文档。
- 后端构建及运行环境升级到 Java 21。
- 删除 MinIO 配置和服务，资料文件改用 NAS Docker 持久化卷与本地文件存储服务。
- 清理 Redis、MinIO 相关配置和编排依赖。
- 增加生产环境配置样例、部署校验、健康检查和失败回滚流程。

### 验证

- 后端测试和打包通过。
- 前后端 Docker 镜像构建通过。
- 生产 Compose 配置及部署脚本校验通过。

### 版本信息

- 分支：`main`
- 关联提交：`99922b2 feat: 优化jenkins部署`
